package:
  name: package_name
  version: 0.0.1

build:
  number: 0
  script: build_output.sh

requirements:
  host:
    - pip
  run:
    - python

outputs:
  - name: output1
    script: build_output.sh
    requirements:
      host:
        - pip
  - name: output2
    script: build_output.sh
    requirements:
      host:
        - pip