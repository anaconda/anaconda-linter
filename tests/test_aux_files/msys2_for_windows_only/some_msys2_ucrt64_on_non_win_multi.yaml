package:
  name: test
  version: 0.1.0

requirements:
  build:
    - msys2-patch # [win]
    - msys2-posix
    - ucrt64-gcc-toolchain
    - ucrt64-xz  # [win]

outputs:
  - name: output1
    requirements:
      build:
        - msys2-patch # [win]
        - msys2-posix
        - ucrt64-gcc-toolchain  # [win]
        - ucrt64-xz  # [win]
  - name: output2
    requirements:
      build:
        - msys2-patch
        - msys2-posix # [win]
        - ucrt64-gcc-toolchain  # [win]
        - ucrt64-xz  # [win]