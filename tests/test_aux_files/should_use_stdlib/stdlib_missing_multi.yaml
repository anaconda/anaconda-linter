package:
  name: test
  version: 0.1.0

requirements:
  build:
    - {{ compiler('go') }}

outputs:
  - name: output1
    requirements:
      build:
        - {{ compiler('cxx') }}
  - name: output2
    requirements:
      build:
        - {{ compiler('fortran') }}