package:
  name: test
  version: 0.1.0

requirements:
  build:
    - {{ stdlib('c') }}

outputs:
  - name: output1
    requirements:
      build:
        - {{ stdlib('c') }}
  - name: output2
    requirements:
      build:
        - {{ stdlib('c') }}