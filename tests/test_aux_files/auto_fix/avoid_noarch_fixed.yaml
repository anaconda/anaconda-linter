package:
  name: test_package
  version: 0.0.1

build:
  number: 0

outputs:
  - name: test_output
    build:
      number: 1
      noarch: python
    requirements:
      host:
      run:
        - python >=3.11

requirements:
  host:
    - python
  run:
    - python

test:
  requires:
    - python
