name: anaconda-linter

dependencies:
  - python
  # build
  - setuptools
  # run
  - ruamel.yaml
  - pyyaml
  - license-expression
  - jinja2
  - tqdm
  - conda-build
  - jsonschema
  - networkx
  - requests
  - crender >=0.5
  # test
  - make
  - pytest
  - pre-commit
  # plug ins
  - pytest-cov
  - pytest-html
