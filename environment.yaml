name: anaconda-linter
channels:
  - defaults

dependencies:
  - python >=3.8
  # build
  - setuptools
  - pip
  # run
  - distro-tooling::percy >=0.1.0,<0.2.0
  - ruamel.yaml
  - license-expression
  - jinja2
  - conda-build
  - jsonschema
  - networkx
  - requests
  # test
  - pytest
  - pre-commit
  # developer tools
  - black
  - isort
  - mypy
  - pylint
  - shellcheck
  # plug ins
  - pytest-cov
  - pytest-html
  - pytest-xdist
